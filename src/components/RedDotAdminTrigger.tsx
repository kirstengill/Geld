import React from 'react';
import { useApp } from '../context/AppContext';

// Replaced global floating button with null. 
// The secret admin trigger is now statically and discretely positioned in the footer as requested.
export const RedDotAdminTrigger: React.FC = () => {
  return null;
};
